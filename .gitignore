# Entorno
.env
backend/.env
frontend/.env

# Python
backend/.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Node
node_modules/
frontend/dist/

# Sistema
Thumbs.db
.DS_Store
.vscode/
.idea/

# Artefactos generados para el movil (se reconstruyen con scripts/exportar_indice_movil.py)
backend/artefactos_movil/

# Distribucion como ejecutable: se reconstruye con PyInstaller a partir del .spec
backend/build/
backend/dist/
# Interfaz ya compilada que el servicio sirve; se genera con `npm run build`
backend/interfaz/
# Base de datos de SQLite que el ejecutable crea en su primer arranque
backend/*.db
